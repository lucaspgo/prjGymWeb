using prjCapeloWeb.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace prjCapeloWeb.DAL
{
    public class DisciplinaDAO
    {
        //private readonly Context _context;
        //public DisciplinaDAO(Context context) => _context = context;
        //public List<Disciplina> Listar() => _context.Disciplinas.ToList();
        //public Disciplina BuscarPorId(int id) => _context.Disciplinas.Find(id);
        //public Disciplina BuscarPorNome(string nome) => 
        //    _context.Disciplinas.FirstOrDefault(x => x.Nome.Equals(nome));
        //public bool Cadastrar(Disciplina disciplina)
        //{
        //    if(BuscarPorNome(disciplina.Nome) == null)
        //    {
        //        _context.Disciplinas.Add(disciplina);
        //        _context.SaveChanges();
        //        return true;
        //    }
        //    return false;
        //}

        //public void Remover(int id)
        //{
        //    _context.Disciplinas.Remove(BuscarPorId(id));
        //    _context.SaveChanges();
        //}

        //public void Alterar(Disciplina disciplina)
        //{
        //    _context.Disciplinas.Update(disciplina);
        //    _context.SaveChanges();
        //}
    }
}
